# 创建 GitHub 仓库说明

## 📋 步骤

1. **进入 cloud-config-tools 目录**
   ```powershell
   cd cloud-config-tools
   ```

2. **运行设置脚本**
   ```batch
   setup_repo.bat
   ```
   
   或手动执行：
   ```powershell
   git init
   git add .
   git commit -m "Initial commit: Cloud Config Tools"
   gh repo create cloud-config-tools --public --source=. --remote=origin --push
   ```

## ✅ 完成

仓库创建成功后，你就可以：
- 克隆仓库：`git clone https://github.com/你的用户名/cloud-config-tools.git`
- 在新电脑上快速安装：运行 `一键安装.bat`

